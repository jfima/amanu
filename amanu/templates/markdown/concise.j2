---
custom_fields:
  summary:
    description: "A concise executive summary of the transcript."
    structure: "string"
  date:
    description: "The date of the conversation or 'Unknown'. This will be overridden by file_date if available."
    structure: "string"
  file_date:
    description: "The creation date of the original file, or the earliest date found in its metadata."
    structure: "string"
  language:
    description: "The primary language of the conversation."
    structure: "string"
  action_items:
    description: "List of tasks, assignments, or next steps mentioned."
    structure:
      type: array
      items:
        type: object
        properties:
          assignee: {type: ["string", "null"]}
          task: {type: string}
  key_takeaways:
    description: "3-5 most important points or conclusions."
    structure: ["string"]
---
# 🚀 Meeting Brief: {{ summary | truncate(50, True) }}

**Date:** {{ file_date | default(date | default('Unknown')) }} | **Language:** {{ language }}

## 📌 Summary
{{ summary }}

## ✅ Action Items
{% if action_items %}
{% for item in action_items %}
- [ ] **{{ item.assignee | default('Anyone') }}**: {{ item.task }}
{% endfor %}
{% else %}
*No actions required.*
{% endif %}

## 💡 Top Insights
{% if key_takeaways %}
{% for item in key_takeaways %}
*   {{ item }}
{% endfor %}
{% endif %}
